# Handoff：项目现状（2026-07-14）

> **当前入口文档。** 上一版：`Information/Handoff_新窗口继续_2026-07-13.md`（已过时）

## 1. 当前状态

功能开发主线（标注 / AI / 音乐 P3 / 书架 / 一键打包）已落地并推到 GitHub。  
**下一窗口优先：P0 人工验收 + P2/P6 backlog**（见 `TODO.md`）。

| 域 | 状态 |
|----|------|
| 阅读 | EPUB / TXT / PDF / CBZ / MOBI(AZW3) 可用 |
| 标注 | EPUB/MOBI/TXT 全套；PDF/CBZ 画笔；侧栏列表 + 书签按书 |
| AI | SSE 流式、按书会话、选区/标注提问、连通性测试 |
| 豆瓣 | 自动 + 手动评分弹窗；封面刷新 |
| 书架 | `lastReadAt` 倒序；Z-Library 外链（`zlib.ch`） |
| 音乐 | **P3 完成**：持久化、添加 URL 弹窗、阅读快捷键、文档流迷你条 |
| 打包 | **`pack.bat`** → `dist/Scroll-{ver}-win-x64.zip` |

## 2. Git 状态

- 分支：`master`（与 `origin/master` **已同步**，2026-07-14 上午已 push）
- 最新提交：`025d0f4 chore: one-click pack tooling and project doc consolidation`
- 工作区：干净
- 远程：https://github.com/chenbinds/Scroll.git
- 本地相对 `df38681` 多推了：`4604fff` → `dc89306` → `025d0f4`

## 3. 项目结构

- 项目根：`D:\00_AIT_Work_Cursor\Projects\03_Scroll\Scroll`
- 技术栈：Electron 31 + React 18 + TypeScript + electron-vite + Zustand
- 关键目录：`src/main`、`src/preload`、`src/renderer`、`scripts/`、`Information/`、`docs/`

## 4. 运行方式

| 用途 | 方式 |
|------|------|
| 开发日常 | `Scroll.vbs`（优先 `release/Scroll.exe`，否则 `out/` + electron） |
| 仅构建 | `rebuild.bat` 或 `npx electron-vite build` |
| **发给别人** | **`pack.bat`** → `dist/Scroll-*-win-x64.zip` |
| 开发调试 | `start.bat` |
| 首次依赖 | `install.bat` |

环境：Node 20+、Windows 10/11 x64。详情：`docs/05-packaging.md`。

验证（PowerShell）：

```powershell
cd D:\00_AIT_Work_Cursor\Projects\03_Scroll\Scroll; npx electron-vite build
```

## 5. 本轮已实现

- 音乐 P3：`musicStorage.ts`、添加 URL 弹窗、阅读快捷键守卫、迷你条文档流布局
- 书架：Z-Library → `openExternal(https://zlib.ch)`；豆瓣评分内联弹窗
- 一键打包：`pack.bat` + `scripts/pack.ps1`（禁代码签名、国内镜像、UTF-8 BOM README）
- 文档：`Handoff_2026-07-14`、`docs/05-packaging.md`、TODO / CLAUDE / README 同步

## 6. 进展与缺口（TODO）

| 优先级 | 内容 |
|--------|------|
| **P0** | 标注人工抽验（EPUB/MOBI/PDF）、冷启动一轮 |
| P2 | DJVU、CBR 压缩 RAR、MOBI 难例回归 |
| P6 | Defender / 杀软误报；可选冒烟测试 |

P3 音乐、P4 AI、P5 豆瓣均已完成。

## 7. 重要决策与约束

- MOBI → **foliate-js**；禁止 Calibre
- 标注不改正文 DOM；离开仅 React 弹窗（`requestLeave`）
- 书签 `bookmarksByBook`；标注 `annotations_{bookId}`
- 音乐：内置 Web Audio generator；不打包侵权音频；本地/URL 只存路径
- 不集成 Listen 1（版权/维护成本；已否决）
- 打包：portable 单文件，关闭 code signing（避免 winCodeSign 权限/下载问题）

## 8. 已知坑

- `pack.bat` 勿用 `npx` 裸调（曾误入 `npm exec` 交互壳）；现已改 `node_modules\.bin\*.cmd`
- 发布说明必须用模板写 UTF-8 BOM（`scripts/README.dist.txt`），勿把中文硬编码进 `.ps1`
- main / preload 变更后：开发态需**完全退出**再开 `Scroll.vbs`；发给他人需重新 `pack.bat`
- PowerShell 串联命令用 `;`，不用 `&&`

## 9. 建议下一步

1. **P0**：按 TODO 做标注抽验 + 冷启动
2. 视验收结果修 bug；再考虑 P2 / P6
3. 不要再大改音乐 / Listen 1

## 10. 文档地图

| 路径 | 内容 |
|------|------|
| `HANDOFF.md` | 根目录索引 |
| `TODO.md` | 主清单 |
| `CLAUDE.md` | Agent 约束 |
| `docs/05-packaging.md` | 打包与分发 |
| `Information/Handoff_标注_2026-07-13.md` | 标注红线 |
| `Information/Handoff_音乐_2026-07-13.md` | 音乐（P3 已完成） |

## 11. 下个窗口启动提示词

见下一节（可直接复制）。
